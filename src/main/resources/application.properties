

# We need to write configurations so that it connects to the mySQL.
# these are standard configurations to connect with DB.

# Mandatory step : Always create database

# Write these properties as it is to make connection whit SQL.

spring.datasource.url=jdbc:mysql://localhost:3306/slms_rocks?createTableIfNotExists=true

spring.datasource.username=root
spring.datasource.password=AnjiAjay143@

# we are writing this thing for hibernate
spring.jpa.hibernate.ddl-auto=update
# it means if the entity has been updated :--> update the SQL table also

spring.jpa.properties.hibernate.globally_quoted_identifiers=true



